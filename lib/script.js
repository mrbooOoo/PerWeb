const progress_python = document.querySelector('.progress-bar-done','python');

progress_python.style.width = progress_python.getAttribute('data-done') + '%';
progress_python.style.opacity = 1;

const progress_c = document.querySelector('.progress-bar-done-c','c');

progress_c.style.width = progress_c.getAttribute('data-done-c') + '%';
progress_c.style.opacity = 1;

const progress_java = document.querySelector('.progress-bar-done','java');

progress.style.width = progress.getAttribute('data-done') + '%';
progress.style.opacity = 1;

const progress_git = document.querySelector('.progress-bar-done','git');

progress.style.width = progress.getAttribute('data-done') + '%';
progress.style.opacity = 1;

const progress_css3 = document.querySelector('.progress-bar-done','css3');

progress.style.width = progress.getAttribute('data-done') + '%';
progress.style.opacity = 1;

const progress_html5 = document.querySelector('.progress-bar-done','html5');

progress.style.width = progress.getAttribute('data-done') + '%';
progress.style.opacity = 1;

const progress_linux_shell = document.querySelector('.progress-bar-done','linux-shell');

progress.style.width = progress.getAttribute('data-done') + '%';
progress.style.opacity = 1;

const progress_office = document.querySelector('.progress-bar-done','office');

progress.style.width = progress.getAttribute('data-done') + '%';
progress.style.opacity = 1;

const progress_autocad = document.querySelector('.progress-bar-done','autocad');

progress.style.width = progress.getAttribute('data-done') + '%';
progress.style.opacity = 1;